package com.myapp.ecosystem;

/** Tiny launcher that just instantiates the controller and runs it. */
public class Main {
    public static void main(String[] args) {
        new Controller().run();
    }
}
